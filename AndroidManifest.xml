<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.idunnolol.repeat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:theme="@style/AppTheme">
        <activity android:name=".app.DemoListActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".app.LayoutDemoActivity" />

        <activity android:name=".app.RepeatedIncludeActivity" />

        <activity android:name=".app.RepeatedInflationActivity" />

        <activity android:name=".app.DetectConfigActivity" />

        <activity android:name=".app.PluralsActivity" />

        <activity android:name=".app.DrawableXmlActivity" />

        <activity android:name=".app.LayerDrawableActivity" />

        <activity android:name=".app.LevelDrawableActivity" />

        <activity android:name=".app.StringFormatActivity" />

        <activity android:name=".app.PreferenceActivity" />
    </application>

</manifest>